import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  modal: {
    flex: 1,
    justifyContent: 'flex-end',
    margin: 0
  },
  content: {
    // minHeight: 100,
    // backgroundColor: '#fff'
  }
})
